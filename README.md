# Погодный ассистент для Ollama

Небольшой CLI-клиент для получения **текущей погоды** по указанному местоположению(юникодное имя любого местоположения на любом языке).

# Быстрый старт

1. Запустите Ollama (если не установлено/не запущено)

```bash
docker compose up -d
```

2. Подтяните модель (пример)

```bash
docker exec -it ollama ollama pull qwen2.5:1.5b
```
> [!IMPORTANT]
> Модель должна поддерживать вызов инструментов. [Список моделей](https://ollama.com/search?c=tools) и [описание поддержки вызова инструментов](https://docs.ollama.com/capabilities/tool-calling).

3. Запустите клиента

```bash
chmod +x ./weather-assistant 
./weather-assistant -m qwen2.5:1.5b
```

# Пример интерактивного запроса

```text
>> Привет, какая погода в Санкт Петербурге?
🤖 Переменная облачность  -5°C
```

# Опции CLI

```text
Simply weather assistant client for Ollama
Usage: weather-assistant --model-name <MODEL_NAME>
Options:
  -m, --model-name <MODEL_NAME>  Ollama model name (must support tool calling)
  -h, --help                     Print help
  -V, --version                  Print version
```